# Public class-imbalance contract

This directory is safe to expose to the bounded Plan compiler. It describes
four registered fixed operations and their allowed configuration. It does not
contain metric formulas, hidden mutations, raw fixtures, or verifier logic.

`public-rationale.md` may explain a generated plan, but it is display-only.
Only schema-valid JSON is accepted by the fixed interpreter.
