# 2:45 demo voiceover and shot list

Record **Live notebook analysis** only after the exact source commit, runner
image digest, Worker version, deployment ID, qualification receipt, and
release-check receipt agree and that deployed tuple passes production smoke. A
bundled sample is frozen, not a substitute live run. Keep **Verified replay**
open as a disclosed backup and never present it as a new model call. Use
[DEMO_CAPTIONS.vtt](DEMO_CAPTIONS.vtt) word for word so the spoken roles,
on-screen labels, and captions agree.

## 0:00–0:12 — The belief break

Open Judge Mode on the disclosed fixed sample. Keep its Sample label visible
and show 98.5% familiar rows beside 59.4% unseen customers. Say: “CounterLab is
for ML learners and instructors. This fixed sample changes one variable—who is
new—and a 98.5% score becomes 59.4%. No model call produced these values.”

## 0:12–0:27 — Question, evidence, and privacy

Move to an exact-qualified **Live notebook analysis**. Start on “What result are
you trying to understand?”, attach the untouched supported notebook, and show
the exact cell/output reference plus privacy summary. Say: “CounterLab begins
with the Question. It treats notebook evidence as untrusted data, never runs a
cell, and sends no raw rows, notebook file, or local path.”

## 0:27–0:43 — GPT-5.6 and Prediction

Show the learner's current explanation and the alternative with equal visual
weight. Confirm that the first captures the learner's meaning. Choose “Remain
near 98%,” set confidence, and seal it. Say: “GPT-5.6 proposes these competing
explanations from only the approved sanitized packet. It cannot run the
notebook or decide the result. The learner seals an immutable Prediction before
any experimental value appears in this live session.”

## 0:43–0:58 — Runtime Codex and Fair Test Builder

Show what changes, what stays fixed, and why whole-customer holdout matches
deployment. If this exact live compile is genuinely rejected and repaired, show
the learner-language explanation and optionally open Evidence & proof for the
sanitized counterexample. If it passes first try, say so. Never stage a failure.
Say: “Runtime Codex composes registered operation IDs into bounded plans. The
fixed scorer selects the test, and the frozen verifier decides whether the
fixed kernel may release evidence.”

## 0:58–1:18 — Experiment Theater

After the verified payload arrives, show the pinned Prediction and one dominant
comparison: 98.5% random rows versus 59.4% unseen customers, with controlled
model and preprocessing. Use the exact table for 67.4% identity ablation, zero
group overlap, sample sizes, seed, and result hash. Say: “The numbers come from
the fixed kernel. GPT-5.6 and Codex may explain or propose, but neither computes
or verifies them.”

## 1:18–1:34 — Boundary

Try one verified Boundary Hunt point, then reveal the full map. Show its
non-color symbols, exact-value table, assumptions, and integrity or signature
wording. Say: “The learner explores already verified cells; the browser and the
model calculate nothing new.”

## 1:34–1:52 — Reflect and Apply

Build: “When rows repeat the same entity, I should hold out whole entities,
because random rows can share identity across train and test.” Then use the
timeline to select a time-ordered holdout and identify the feature that crosses
NOW. Say: “No model grades the sentence. The fixed evaluator checks transfer on
a different surface.”

## 1:52–2:10 — Repair

Show the repair preview: random rows become whole-customer holdout, identity is
removed, and overlap is reported; target, model family, unrelated cells, and the
original notebook remain preserved. Review the verified copied-notebook patch
and use the primary download. Say: “Repair stays locked until deterministic
transfer passes, and CounterLab patches only a copy.”

## 2:10–2:30 — Completion and proof

Show: “You completed one verified entity-leakage loop.” Then show “good on
familiar rows” was compared with “performance on new entities in this fixed
task,” followed by “Fixed transfer task passed” and the one-task/no-mastery
limitation. Open Evidence & proof for the Reasoning Diff and Proof Capsule.
Say: “The Reasoning Diff is for the learner. The Proof Capsule binds the
machine-readable evidence, versions, limitations, and event chain.”

## 2:30–2:45 — Build truth and close

Show the exact release identity. Say: “Codex also accelerated the build through
repository exploration, tests, and bounded repairs; humans fixed the scope and
authority rules. Learner-impact status is NO_DATA, so we claim no measured
learning gain. Chatbots explain. CounterLab lets reality answer.”

## Backup disclosure

If the live runner is unavailable, say: “This is a Verified replay of an actual
stored run; it makes no new model call.” Keep the replay label visible on every
screen. Do not splice replay footage into a sequence labelled live, and do not
operate replay or sample controls as though they request a new calculation.
