# Screenshot plan

Status: planned, not captured for the final release. Repository-contained
CloakBrowser is available and passes the current local journey matrix, but no
exact public release endpoint and deployment receipt exist yet. Local
CloakBrowser and stock-Chromium design-review artifacts cannot qualify or
supply the final submission images. Do not reuse historical or local
screenshots as evidence of the frozen public release.

Capture the current commit through CloakBrowser at 1440 × 900 and 390 × 844.
Add a 1280 × 720 overflow check even if it is not a submission image. Use real
product state and omit browser chrome where the submission permits.

## Curated Devpost package

Create exactly four primary assets plus one thumbnail from the same qualified
public tuple:

1. `01-judge-belief-break-1440x900.png` — the primary Judge image: learner
   claim, persistent Fixed sample label, one changed evaluation unit, held
   controls, 98.5% familiar rows, 59.4% unseen customers, Boundary consequence,
   and learner benefit in the first viewport.
2. `02-live-prediction-and-authority-1440x900.png` — supported live notebook
   evidence, approved privacy packet, equal-weight models, sealed Prediction,
   and truthful GPT-5.6/Codex/kernel/verifier provenance. Capture only after a
   genuine live call and exact release binding.
3. `03-theater-boundary-1440x900.png` — pinned Prediction, one verified result,
   controlled variables, Boundary Hunt/full-map handoff, non-colour symbols,
   and the exact-value table.
4. `04-transfer-repair-proof-1440x900.png` — deterministic transfer outcome,
   minimal repair preview, copied-notebook download, Reasoning Diff, and Proof
   Capsule action.
5. `thumbnail-judge-belief-break.png` — a legible crop derived from asset 1;
   do not introduce different values, claims, or release identity.

Store final images under
`docs/submission-assets/<deployment-receipt-id>/`. Beside every PNG, write a
same-basename JSON sidecar containing: source commit, runner image digest,
Worker version, deployment ID, deployment receipt hash, public URL and route,
viewport, capture timestamp, browser authority, PNG SHA-256, console errors,
and failed requests. A missing or mismatched field keeps the asset out of the
submission.

Capture desktop and mobile variants for qualification even when only the five
curated files are uploaded. The 390 × 844 Judge capture must prove that the
claim, comparison, Boundary consequence, and learner benefit remain visible
without horizontal overflow.

## Audit coverage matrix

1. **Question-first landing** — “What result are you trying to understand?”,
   the claim field, notebook attachment, sample prompt chips, support sentence,
   and secondary Judge Mode action. Do not reveal a result above the fold.
2. **Notebook evidence story** — headline notebook metric, exact cell/output
   references, plain relevance, read-never-run trust, and collapsed integrity
   detail.
3. **Privacy and Model Duel** — the “CounterLab will send” summary beside the
   equal-weight current and alternative explanations. Capture the exact packet
   only in a separate disclosure without sensitive content.
4. **Prediction Seal** — notebook score beside the categorical expected
   intervention result, confidence control, and persistent gold sealed summary.
5. **Fair Test Builder** — changed versus held-fixed variables, deployment
   match, verification progress, and one genuine rejection/repair only if it
   occurred. Never stage a rejected plan for the screenshot.
6. **Experiment Theater** — deep-navy canvas, pinned prediction, one large
   verified comparison, one-sentence finding, controlled-variable statement,
   and collapsed Evidence & proof.
7. **Boundary Hunt and map** — attempted verified cell, fixed evidence-linked
   hint, then the full non-color Boundary Map with exact-value table,
   assumptions, non-claims, and receipt disclosure.
8. **Apply** — Reflection Builder plus the entity-leakage timeline or imbalance
   cost matrix, including the locked-patch state and deterministic pass state.
9. **Repair and completion** — learner-facing repair preview, preserved scope,
   capability-first completion, primary notebook-copy download, Reasoning Diff,
   and Proof Capsule action.
10. **Mode and refusal evidence** — persistently labelled Verified replay,
    unsupported notebook reasons with disabled advance, and one class-imbalance
    journey. Keep these as separate captures so mode authority is unmistakable.

For mobile, include `Step n of 6`, the accessible progress dialog, the fixed
help action, 44 px controls, and no horizontal overflow. Capture reduced-motion
parity as test evidence rather than trying to imply motion in a still image.

Do not capture synthetic result values, unsigned output labelled signed, an
unlabelled replay, private model reasoning, credentials, local paths, raw
notebook content, or DevTools-only evidence.
