# Evaluation

## Deterministic fixture

Seed 1729 creates 480 customers × 6 observations = 2,880 rows. Customer identity
is a strong shortcut under row-wise splitting; non-identity features contain a
weak real signal. The notebook's stored output is generated by the actual
fixture/kernel build script.

## Current computed result

| Run                        | Accuracy |  ROC AUC | Test n | Customer overlap |
| -------------------------- | -------: | -------: | -----: | ---------------: |
| Random row split           | 0.984722 | 0.983804 |    720 |       389 (100%) |
| Whole-customer group split | 0.594444 | 0.641243 |    720 |           0 (0%) |
| Identity ablation          | 0.673611 | 0.725007 |    720 |       389 (100%) |

Canonical result hash:
`2501654264b9aa85b39fca944e585ff9b04263b83e182bc186d1f16464fee3b0`.

Assertions use broad credible ranges, ordering margins, overlap, and deterministic
hashes. No UI percentage is a handwritten experimental result.

## Mutation benchmark

`./scripts/run-mutations.sh leakage` actively checks 12 published mutations.
The current achieved metric is 12/12 detected. The mutation IDs and expected
invariants are emitted by the script; a missed critical mutation exits nonzero.

## Live compiler evidence

The first authenticated `gpt-5.6-sol` run was rejected on an unsupported SDK
constructor. Repair 1 corrected it but left `__pycache__`; repair 2 did not
remove that existing directory. The exact-file policy rejected the run and no
result was authorized.

A separate later run, after public SDK documentation was corrected, produced an
adapter hash `1b5cb8d48621372ea6fb0894114cd6054046481e1f0764a2c8a49126e6504adb`.
It reproduced the canonical result, passed 18 verifier invariants, and detected
12/12 mutations. This is not presented as a third repair.

## Transfer and patch

The fixed forecasting transfer passes only for a time-ordered holdout plus
identification of the centered rolling target as future leakage. Failure keeps
the patch locked. The public patch changes cell 3, preserves four unrelated cell
source hashes, uses whole-customer grouping, removes customer identity, produces
zero overlap, and reproduces deterministically.

## Test surfaces

- Pytest: fixture, kernel, canonicalization, parser policy support, transfer,
  patch verifier, runner/AST/Docker policy, verifier invariants and service API.
- Vitest: contracts, analyst rejection, evidence resolution, transitions, event
  chain, replay, Codex protocol/sanitizer, Worker API and React behavior.
- Playwright with CloakBrowser: instant path, immutable prediction, session and
  replay refresh restoration, persistent replay label, read-only stage review,
  explicit reset, focused phase transitions, no result before prediction,
  transfer fail/pass lock, Proof Bundle, live setup, mobile keyboard flow, and
  unsupported upload.

`docs/ACHIEVED_METRICS.json` is generated by executing the kernel/mutation code.
No learner-study, retention, mastery, or causal learning outcome is claimed.
