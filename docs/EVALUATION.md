# Evaluation

## Deterministic fixture

Seed 1729 creates 480 customers × 6 observations = 2,880 rows. Customer identity
is a strong shortcut under row-wise splitting; non-identity features contain a
weak real signal. The notebook's stored output is generated by the actual
fixture/kernel build script.

## Current computed result

| Run                        | Accuracy |  ROC AUC | Test n | Customer overlap |
| -------------------------- | -------: | -------: | -----: | ---------------: |
| Random row split           | 0.984722 | 0.983804 |    720 |       389 (100%) |
| Whole-customer group split | 0.594444 | 0.641243 |    720 |           0 (0%) |
| Identity ablation          | 0.673611 | 0.725007 |    720 |       389 (100%) |

Canonical result hash:
`2501654264b9aa85b39fca944e585ff9b04263b83e182bc186d1f16464fee3b0`.

Assertions use broad credible ranges, ordering margins, overlap, and deterministic
hashes. No UI percentage is a handwritten experimental result.

## Mutation benchmark

`./scripts/run-mutations.sh leakage` actively checks 12 published mutations.
The current achieved metric is 12/12 detected. The mutation IDs and expected
invariants are emitted by the script; a missed critical mutation exits nonzero.

## Class-imbalance anti-template proof

Seed 2603 creates 6,000 rare-event rows with 65 positives (1.0833%
prevalence). The computed majority baseline reaches 0.989333 accuracy while its
rare-class recall is 0. The stratified model's PR-AUC is 0.196393, and lowering
the bounded threshold produces 0.3125 recall. These values are regenerated by
the fixed kernel, not copied into UI authority.

`./scripts/run-mutations.sh imbalance` detects 12/12 critical mutations,
including inconsistent confusion totals, a literal majority baseline, fake
PR-AUC, an inert threshold, prevalence drift, and nondeterministic hashes.

## Held-out v2 matrix

`pnpm run held-out:run` safely parses four leakage notebooks, four imbalance
notebooks, and two typed refusal cases. Intake/routing checks pass 10/10.
Supported cases then run a labelled `deterministic_contract_probe` through the
independent Plan verifier, fixed kernel, fixed transfer evaluator, and fixed
copy-patch verifier without executing uploaded notebook cells. Seven of eight
complete; the Random Forest notebook reaches verified result and transfer but
its patch is refused by the registered logistic-only non-sample leakage patch.

This probe measures deterministic authority coverage. It does not call GPT or
Codex and is not reported as live plan-generation success. Human review labels
remain pending. Exact hashes, durations, failure code, and limitations are in
`docs/HELD_OUT_RESULTS.json` and `docs/HELD_OUT_MATRIX.md`.

## Live compiler evidence

The first authenticated `gpt-5.6-sol` run was rejected on an unsupported SDK
constructor. Repair 1 corrected it but left `__pycache__`; repair 2 did not
remove that existing directory. The exact-file policy rejected the run and no
result was authorized.

A separate later run, after public SDK documentation was corrected, produced an
adapter hash `1b5cb8d48621372ea6fb0894114cd6054046481e1f0764a2c8a49126e6504adb`.
It reproduced the canonical result, passed 18 verifier invariants, and detected
12/12 mutations. This is not presented as a third repair.

## Transfer and patch

The fixed forecasting transfer passes only for a time-ordered holdout plus
identification of the centered rolling target as future leakage. Failure keeps
the patch locked. The public patch changes cell 3, preserves four unrelated cell
source hashes, uses whole-customer grouping, removes customer identity, produces
zero overlap, and reproduces deterministically.

## Test surfaces

- Pytest: fixture, kernel, canonicalization, parser policy support, transfer,
  patch verifier, runner/AST/Docker policy, verifier invariants and service API.
- Vitest: contracts, analyst rejection, evidence resolution, transitions, event
  chain, replay, Codex protocol/sanitizer, Worker API and React behavior.
- Playwright with CloakBrowser: historical released journeys and the current
  fail-closed v6.1 specification described separately below.

### Historical executed browser evidence

Two opt-in browser tests upload an untouched held-out leakage notebook and the
public imbalance notebook. Each requires real configured GPT,
Container/Codex, fixed kernel, verifier, interactive run, transfer,
copied-notebook patch, proof route, and downloads. They run only with
`COUNTERLAB_E2E_LIVE=1`; no request interception or sample substitution is
used. `scripts/production-smoke.sh` combines both with health, sample/replay,
and public-asset secret checks. Worker
`7c67c0f4-a4cb-4503-80b0-5a5bd491f3ab` ran all seven stages successfully on
2026-07-16; the exact artifact, result, patch, event-chain, Proof Bundle, and
scientific-engine authority hashes are recorded in
`docs/PRODUCTION_SMOKE.json` (SHA-256 `cd5c0c05…`). Later deployments must rerun
the smoke and cannot inherit that version's authority.

Those completed journeys qualify only their recorded source and deployment.
They are not browser evidence for branch `feat/learner-ux-v6.1`.

### Current v6.1 local specification

The current Cloak-only Playwright suite statically collects 40 tests from three
spec files. It covers the desktop/mobile viewports, keyboard/focus, reduced
motion, refresh phases, sample, replay, mocked live, unsupported intake,
rejected verification, failed transfer, successful patch, downloads, Boundary
Hunt, exact tables, no horizontal overflow, no model call from local controls,
fresh-session recovery after both learner terminal responses, browser
back/forward, absent and malformed upload, same-file retry, and lost private
session response recovery without re-upload.

Zero of those tests ran in this pass because `CLOAK_CDP_ENDPOINT` was absent.
The harness exited with status 1 and refused to launch stock Chromium. No
current-branch screenshot, screen-reader session, rendered accessibility audit,
or Web Vitals result is claimed.

Current source-candidate checks passed web Vitest (58 files, 360 tests),
repository, web, and Worker TypeScript, the 231-test Python kernel/runner gate,
the Vite/Worker build, and a 741-file secret scan that excludes generated
browser-profile state. Root Vitest passes 486/488; the only two pending tests
are the source/image-bound internal integrity and lock evidence checks that run
after the immutable image build.

The successful build reported a 1,695.34 kB Worker bundle (328.63 kB gzip),
177.70 kB main client CSS (31.21 kB gzip), a lazy 14.58 kB Reasoning Diff
JavaScript chunk (4.47 kB gzip) plus 4.63 kB CSS (1.38 kB gzip), and a 398.22
kB main client chunk (115.88 kB gzip). The main chunk no longer triggers the
over-500 kB warning. Runtime performance was not measured.

Full evidence and remaining browser checks are in
[`LEARNER_UX_V6_1_EVIDENCE.md`](LEARNER_UX_V6_1_EVIDENCE.md).

`docs/ACHIEVED_METRICS.json` is generated by executing the kernel/mutation code.
No learner-study, retention, mastery, or causal learning outcome is claimed.
